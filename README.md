
  # Recreate Design with Interactions

  This is a code bundle for Recreate Design with Interactions. The original project is available at https://www.figma.com/design/BjfZLZXaJE5Lbr43kBarQF/Recreate-Design-with-Interactions.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  